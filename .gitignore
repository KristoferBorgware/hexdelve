# Generated audio. ~30 MB each, and reproducible from the scripts beside them:
#   node assets/audio/dungeon-crawl.js
# Committing them would make the history ~300x larger than the source.
*.wav
.mixgain-*

# Dependencies and build output
node_modules/
dist/
dist-app/
dist-lib/
dist-types/
release/
*.tsbuildinfo
.vite/

# IDE
.idea/
.vscode/

# OS
.DS_Store

# Claude Code local settings (launch.json is tracked on purpose)
.claude/settings.local.json
