# Generated audio. ~30 MB each, and reproducible from the generators that sit
# beside them in public/assets/audio:
#   npm run audio
# Committing them would make the history ~300x larger than the source.
*.wav
.mixgain-*

# Dependencies and build output
node_modules/
dist/
dist-app/
dist-lib/
dist-scripts/
dist-types/
release/
*.tsbuildinfo
.vite/

# IDE
.idea/
.vscode/

# OS
.DS_Store

# Claude Code local settings (launch.json is tracked on purpose)
.claude/settings.local.json
