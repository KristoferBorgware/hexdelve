id: sword
name: Sword
tags: [weapon]

object:
  components:
    - { type: rig, rig: ../rigs/humanoid.rig.yaml }
    - { type: mesh, mesh: ../meshes/sword.mesh.yaml }
    - { type: attach, bone: handR, lift: 0.04, tilt: pi / 2 }
    - { type: item }
